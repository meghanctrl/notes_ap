# Simple Flask CRUD

Very simple notes CRUD app built with Flask and SQLite.

## Setup

```bash
uv venv
uv sync
```

## Run

```bash
uv run flask --app app run --debug
```

Open `http://127.0.0.1:5000`.

## Test

```bash
uv run pytest
```
